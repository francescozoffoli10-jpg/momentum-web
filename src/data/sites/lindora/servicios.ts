import type { Tenant } from '@/data/types'

// Lindora does not have a dedicated "Servicios" directory section on the live site.
// These are tenants that appear in the main directory under general services
// (banks, wellness, fitness, etc.) scraped from momentumlindora.com.
export const servicios: Tenant[] = []
